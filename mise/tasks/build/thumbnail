#!/usr/bin/env bash
# [MISE] description="Render thumbnail title page as thumbnail.png"
set -euo pipefail

typst compile \
    --root . \
    --font-path fonts \
    --ppi 300 \
    thumbnail/thumbnail.typ \
    thumbnail/thumbnail.png

echo "  → thumbnail/thumbnail.png"
